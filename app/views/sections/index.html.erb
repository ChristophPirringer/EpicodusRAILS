<h1>Browse the sections</h1>

<% if @sections.any? %>
  <ul>
    <% @sections.each do |section| %>
      <li><%= link_to section.name, section_path(section) %></li>
    <% end %>
  </ul>
<% else %>
  <p>No sections yet.</p>
<% end %>

<p><%= link_to "Create new sections", new_section_path %></p>
